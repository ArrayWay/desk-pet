# 浮光橙仔开发计划

## 产品目标

把现有的动画桌宠完善为一个低打扰、可持续扩展的桌面开发助手：平时有自然的陪伴感，关键开发事件能给出明确反馈，并能通过桌宠快速触发常用 VS Code 操作。

## 设计原则

- 不抢焦点：默认不弹出阻塞窗口，不打断输入。
- 状态可恢复：临时动作结束后必须回到待机或行走状态。
- 功能可关闭：自动行走、事件联动、提醒和提示音均应可配置。
- 协议先行：桌面端与 VS Code 扩展通过明确的消息协议通信。
- 工作区本地开发：构建、缓存、发布和验证产物统一保存在项目目录。
- 依赖目录约束：新增软件、SDK、包管理器缓存或工具只能安装到 `D:\ai_tools\ai_sandbox\AppData\Local`；未经允许不得写入 C 盘用户目录。
- Python 图像工具统一使用项目根目录的 `.venv-1`，不再保留或新增其他虚拟环境目录。
- 动画渐进增强：功能先使用现有动画提供完整降级体验，专用动画由用户提供后再接入，不阻塞工程功能。

## 里程碑

### M1 交互与运行稳定性

- [x] 透明窗口与透明图集显示。
- [x] 消费 `durationMs`，临时动作到期后恢复环境状态。
- [x] 非循环动作结束后恢复行走方向，而不是固定回到待机。
- [x] 鼠标悬停时暂停自动移动，离开后恢复。
- [x] 拖拽松手后吸附附近屏幕边缘。
- [x] 重复启动时唤醒已有桌宠实例。
- [x] 校验动画配置和裁帧边界，启动失败时给出明确原因。
- [x] 鼠标按左、中、右方向分区提供轻微视线/侧头反馈，使用迟滞避免抖动。
- [x] 拖拽时使用提起反馈，松手后播放落地反馈并提供有限惯性。
- [x] 使用当前帧 Alpha 命中测试，透明区域不触发身体交互。
- [x] 连续点击在挥手、检查和跳跃之间轮换，并保留双击跳跃。
- [x] 空闲行为使用权重、冷却时间和最近动作去重。

验收标准：连续触发编辑、保存、任务和调试事件时动作不会永久卡住；重复执行启动命令不会产生多个实例；桌宠可稳定拖拽并保持在工作区范围内。

### M2 设置与状态持久化

- [x] 保存桌宠位置、自动行走、置顶、移动速度和动画频率。
- [x] 托盘菜单使用勾选状态呈现开关值。
- [x] 支持多显示器位置恢复和屏幕断开后的边界修正。
- [x] 增加重置设置与恢复默认位置。
- [x] 设置文件保存在程序目录的 `Data` 子目录，不写入系统盘用户目录。
- [x] 保存番茄钟统计、提醒开关、心情、精力、亲密度和当日日期。

验收标准：重启后恢复有效设置；显示器布局变化后桌宠仍可见；设置文件损坏时自动回退默认值。

### M3 双向 VS Code 联动

- [x] 扩展与桌面端支持双向 IPC。
- [x] 桌宠右键菜单可打开 Problems、源代码管理、终端和当前项目。
- [x] 构建失败时点击桌宠打开 Problems。
- [x] 增加桌面端显示、隐藏、暂停、退出命令。
- [x] Webview 从动画配置读取图集尺寸，移除硬编码。
- [x] 右键菜单支持运行默认构建任务、运行默认测试任务和开始专注。
- [x] Git 存在未提交改动时低频反馈，不重复刷屏。
- [x] 等待用户确认/输入时使用 `waiting`，任务结束时按退出码显示成功或失败。

验收标准：桌面端命令能准确作用于当前 VS Code 窗口；IPC 断开不会导致任一进程崩溃。

### M4 专注与提醒功能

- [x] 番茄钟预设 `25/5`、`50/10` 和自定义时长。
- [x] 久坐、喝水和护眼提醒，可独立关闭。
- [x] 专注期间降低动作频率，到点后使用动画和系统通知提醒。
- [x] 保存当天专注次数和累计时长。
- [x] 支持“20 分钟后提醒我”的一次性提醒。
- [x] 提供久坐、喝水、护眼提醒开关，并支持点击桌宠确认。
- [x] 自定义专注/休息时长由 VS Code 设置提供，托盘菜单直接启动。

验收标准：计时跨窗口切换稳定；提醒不抢焦点；暂停和恢复行为符合预期。

### M5 角色与动画扩展

- [x] 增加拖起、落地、伸懒腰、睡觉和庆祝动作。
- [x] 空闲行为使用权重、冷却和最近行为去重。
- [x] 增加轻量心情与精力状态，只影响动作选择和提示频率。
- [x] 为新增动画执行图集透明度、边界和逐帧 QA。
- [x] 根据连续工作时间、构建结果和互动频率更新心情与精力。
- [x] 专注完成和任务成功积累亲密度并解锁更丰富的空闲动作权重。
- [x] 关键事件使用透明、不抢焦点的独立气泡窗口，且可关闭。
- [x] 补充拖起、落地、伸懒腰、坐下、睡觉和庆祝专用动画。

### M6 工程能力与发布

- [x] 状态调度器支持优先级、可打断性、持续时间和环境状态恢复。
- [x] 双向 IPC 断线自动降级，消息异常写入工作区本地日志。
- [x] 设置入口覆盖移动、动画、提示、番茄钟、启动和显示器偏好。
- [x] 单实例保护和重复启动唤醒。
- [x] 增加桌面端行为测试、扩展协议测试和发布前 smoke test。
- [x] 发布产物同步到扩展目录，完成单实例、透明度、多屏和 IPC 实机验证。

验收标准：无需外部服务即可运行全部基础功能；IPC 或配置损坏不会使桌宠退出；日志、设置、构建和测试产物均遵守存储约束。

### M7 真实宠物狗玩伴与双宠互动

- [x] 建立独立的真实宠物狗玩伴开发计划。
- [x] 完成独立透明玩伴窗口、设置持久化、托盘召唤和基础跟随框架。
- [x] 接入用户提供的真实宠物狗身份基准图。
- [x] 完成跟随追逐、摸头回应、共同休息、专注守护和开发任务联动。
- [x] 完成抛球取回、找 Bug、藏起来、晨间来访和成长称号等后续玩法。
- [x] 抽象单访客系统，为猫、鸟等后续玩伴保留扩展能力。

详细范围、素材批次、行为优先级、数值规则和验收标准见 [DOG_COMPANION_DEVELOPMENT_PLAN.md](DOG_COMPANION_DEVELOPMENT_PLAN.md)。玩伴使用真实宠物摄影风格，不采用 Q 版或卡通化造型；所有动作素材按实际开发阶段生成，避免重复或提前生成无用图片。

### M8 访客目录与扩展接入

- [x] 将内置访客从单分支查找提升为大小写不敏感的 Profile 注册表，并公开只读访客目录。
- [x] 增加 Profile 一致性和必需资源校验，错误会指出访客 ID、状态或资源路径。
- [x] 建立通用访客切换生命周期，切换前完整停止旧窗口、互动和道具。
- [x] 仅在注册多个访客后显示访客选择入口；单访客时保持现有菜单和交互不变。
- [x] 为后续访客提供接入清单和自动化验证，不在身份素材确认前预生成猫、鸟资源。

验收标准：新增访客只需注册 Profile 和资源即可进入统一生命周期；无效 Profile 不影响主宠启动；切换访客后不会残留窗口、道具或旧访客状态。

### M9 第二只宠物狗与训练互动

- [x] 确认第二只狗的稳定访客 ID `training-dog`、显示名称“训练犬”、身份基准照片和外观锁定描述。
- [x] 第一阶段接入“半坐伸舌”招呼、握手邀请与握手成功反馈；握手入口和前爪命中状态已接入，待发布包人工验收。
- [x] 第一阶段将“喂食”拆分为无冷却零食和有冷却狗粮：零食提供短反馈和少量亲密度，狗粮执行嗅闻、低头进食和舔嘴感谢并恢复精力；狗与食盆使用同一透明动画帧，不增加货币、库存或付费养成，待发布包人工验收。
- [x] 第二阶段接入截飞盘流程：拖动确定方向与力度，按落点显示器约束抛物线，等待观察后跑向预测交点，成功时腾空截住、落地和叼回，失败时扑空回望；高优先级事件可完整中断并清理飞盘。
- [x] 为第二只狗扩展通用能力、动作状态和道具资源契约，不在主窗口增加按访客 ID 分支。
- [ ] 完成第二 Profile 注册、访客切换、资源降级、发布打包和多显示器/高 DPI 人工验收；Profile、资源、第一阶段代码与发布已完成，仅人工验收待完成。

验收标准：第二只狗拥有可辨识的主动训练互动；握手、喂食和飞盘均有明确开始、成功、取消和冷却状态；切换、隐藏或退出时不残留狗粮、飞盘、计时器或旧访客窗口。

### M11 开发陪伴玩法与联动增强

- [ ] P0 收口 M9 飞盘/多显示器/中断清理和 M10 养成系统长时间运行、发布包人工验收。
- [x] P1 VS Code 专注双端闭环代码完成：统一 `focus-start` 参数、回传 `focus-state`、接通 `BreakMinutes`，并补充任务/测试结果反馈和 `morning-check` 日戳语义；统一验收待执行。
- [x] P2 主宠状态条、精力联动提醒、工作区定位、训练犬握手/飞盘彩蛋和半坐招呼代码完成；高优先级中断与养成开关统一验收待执行。
- [x] P3 提交仪式、长会话陪伴、点击连招、短拖安抚、备注气泡、访客护理和找 Bug 反馈代码完成；统一验收待执行。
- [x] P4 单访客槽位换班、能力门控和训练犬资源接入代码完成；新物种保持不注册，人工验收待执行。

详细范围、实现面、阶段顺序、验收标准和明确不做项见 [PLAY_FEATURES_EXECUTION_PLAN.md](PLAY_FEATURES_EXECUTION_PLAN.md)。本里程碑继续保持单访客槽位，不引入主宠喂食、养成货币、双访客同屏或复杂 AI 对话。

## 动画资源缺口

当前图集包含 15 个状态：原有 9 个状态，以及 `picked-up`、`landing`、`stretching`、`sitting`、`sleeping` 和 `celebrating`。新增素材已完成透明化、逐帧检查、图集验证和运行时接入。

验收标准：长时间运行无明显动作重复；所有动画透明背景正常；状态切换无闪烁和错帧。

## 当前迭代

### v1.2.1 发布记录

- [x] 新增托盘菜单一键开启或关闭自动运行桌宠，并持久化设置。
- [x] 新增主宠一键换装，支持默认外观与人物2整套皮肤切换。
- [x] 将人物2透明动画资源按皮肤状态目录打包到桌面端和 VS Code 扩展运行时。
- [x] 为人物2缺少的互动动作增加兼容状态映射，避免换装后动作显示默认人物。
- [x] 修复人物2互动期间回退到人物1的身份一致性问题。
- [x] 桌面端与 VS Code 扩展版本统一升级为 `1.2.1`。
- [x] Release 构建、扩展检查、桌面 smoke test 和人物2启动/互动路径验证通过。

M7、M8 已完成并通过自动化与人工验收。M9 已完成第二个访客的身份素材、两阶段动作素材 QA、正式资源部署、`training-dog` Profile 注册、握手、喂食与截飞盘状态机及发布验证；飞盘使用独立道具窗口和 9 组动作，消耗 20 点精力，并按预测落点显示器限制轨迹与截点；首狗能力与普通取球 12 点精力规则保持不变。下一步完成近投、远投、过高、不可达、连续取消和高 DPI 多显示器人工验收。

M10 养成系统首版已完成 P0–P3 代码落地（Release 构建通过），范围与验收见 [GROWTH_SYSTEM_EXECUTION_PLAN.md](GROWTH_SYSTEM_EXECUTION_PLAN.md)。已实现：`Stamina`/`Satiety` 拆分、访客 `Stats` 分桶与旧进度 50/50 迁移、`GrowthService` 统一结算、软门槛、桌面数字状态条与托盘开关。P4 已完成代码侧收口与自动回归（软门槛减半可到 0、恢复/衰减统一走 GrowthService、状态条防重叠）；长时间运行与多显示器/发布包人工验收仍待完成。状态条已分主宠浅橙/访客蓝色主题；托盘增加「养成系统」总开关（关闭封存数值，开启后可显示/隐藏状态条）。

M11 P1-P4 代码已完成并进入统一验收阶段；P0 仍覆盖 M9/M10 长时间运行、多显示器、高 DPI、中断清理和发布包人工验收。新增玩法继续遵守 `GrowthService` 统一结算、`GrowthEnabled` 封存、单访客生命周期和 Release 构建自动同步发布目录的工程约束。

## 工程验证

```powershell
$env:DOTNET_CLI_HOME = "$PWD\tmp\dotnet-home"
$env:NUGET_PACKAGES = "$PWD\tmp\nuget-packages"
dotnet build .\desktop-wpf\Fuguang.DesktopPet\Fuguang.DesktopPet.csproj -c Release
npm run check --prefix .\vscode-extension
```

日常启动入口：`publish/windows/release/Fuguang.DesktopPet.exe`。`dotnet build` 会通过 csproj 的 `SyncLaunchDirectories` 自动把输出同步到 `publish/windows/{debug|release}`；`Release` 还会同步 `vscode-extension/desktop`，避免启动旧路径。完整 VSIX / `publish/vscode/*` 仍用 `tools\publish.ps1`。代理侧强制约定见 [`.github/AGENTS.md`](.github/AGENTS.md)。

发布目录统一为项目内的 `publish/windows/{debug,release}` 和 `publish/vscode/{debug,release}`。
四个目录只保留各配置的最新构建；`vscode-extension/desktop` 是扩展源码包内置的桌面运行时资源，不作为独立发布目录。

`desktop-wpf` 是 WPF 桌宠源码工程，必须保留；其中的 `bin` 与 `obj` 是可再生构建缓存。`tmp` 保存图像分析、逐帧 QA、构建临时文件和项目内依赖缓存，可整体删除；删除后下次构建会重新创建缓存，并可能需要恢复 NuGet 与 npm 依赖。

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\tools\smoke-test.ps1 -PublishDirectory .\publish\windows\release
```

本轮验证记录：

- [x] `dotnet build` Release 通过，访客模型、窗口与主槽位无编译错误。
- [x] `npm run check --prefix .\vscode-extension` 通过。
- [x] `tools\publish.ps1 -Configuration Release` 完成 Windows 发布与 VSIX 打包。
- [x] 增强 smoke test 通过，覆盖访客资源、通用设置结构、IPC、显示隐藏、退出和单实例。
- [x] 在 100% 与高 DPI、多显示器和显示器断开场景完成窗口与球落点人工验收。
- [x] M8 Profile 注册表与资源校验阶段完成，桌面端 Release 构建通过；发布目录需在 M8 阶段收尾后重新生成。
- [x] M8 资源校验已纳入 smoke test；重新发布的 Windows Release 与 VSIX 通过验证，下一步进入访客切换生命周期。
- [x] M8 访客切换生命周期已接入并重新发布；最新 Release smoke test 通过，单访客行为保持不变。
- [x] M8 多访客选择入口已接入：注册数大于 1 时显示选择菜单，切换后同步 Profile 名称和能力菜单；当前仍只有狗狗 Profile，真实多访客点击验收待第二个合法 Profile 接入。
- [x] M8 接入指南已完成，资源打包、Profile 注册、自动验证和多访客人工验收见 [VISITOR_PROFILE_GUIDE.md](VISITOR_PROFILE_GUIDE.md)。
- [x] M9 第一阶段正式资源 QA 通过：9 组校验状态均为 6 帧、`192x208`、透明且未触碰安全边缘。
- [x] M9 第一阶段 Release 构建、扩展检查、Windows/VSIX 发布和增强 smoke test 通过；Windows Release 与 VSIX 均包含训练犬动作和食盆资源。
- [x] 修复训练犬基础图曾解析到 `Companions\training-dog.png` 的 Profile 路径错误；smoke test 现在以启用的 `training-dog` 设置启动发布程序，覆盖实际 Profile 资源校验和窗口创建。
- [x] 重建被源参考图裁断的训练犬食盆为完整 `192x144` 透明 4:3 资源；正式 `food-sniff` 与 `eating` 帧已将完整食盆和狗合成在同一透明位图中，避免独立顶层窗口产生视觉错层。
- [x] 修复首狗“找 Bug”动作的等宽分槽裁切：提取器新增整行连通主体模式，`sniffing-right` 6 帧已重建并通过尺寸、透明边缘、联系人表、Release 发布和 smoke test 验证。
- [x] 喂食入口改为托盘单击直接开始；冷却由 2 分钟缩短为 30 秒并显示剩余秒数，忙碌状态不再静默失败。
- [x] 喂食入口拆为“喂零食（无冷却）”和“喂狗粮”；零食不恢复精力且不写入冷却，专用动作素材生成前复用短促舔嘴反馈。
- [x] 使用 `SECOND_DOG_IMAGE_PROMPTS.md` 的 `second-dog-treat-eating-reference.png` 提示词生成、提取并接入专用零食动作帧。
- [x] M9 第一阶段人工验收：确认前爪命中区、喂食合成帧观感以及高 DPI 下的显示和中断清理。

## 总 Review 收口（2026-07-25）

- [x] 已核对训练犬当前交互：左键单击启动握手邀请，邀请期间点中前爪完成握手；右键触发 `petting-response`，使用 `scale=1.15`、`verticalOffset=0` 的轻微脸部特写，900ms 后恢复。
- [x] 已修复发布 smoke test 漏检：补充 `handshake-spin` 与 `frisbee-showoff-with-disc` 两个已注册状态目录的逐帧检查。
- [x] 已同步训练犬开发计划、访客接入指南和 GitHub 发布 README，移除训练犬仍使用左键摸头的过期描述，并统一扩展版本为 `1.2.0`。
- [x] 自动化验证基线：Release 构建、VS Code `npm run check`、Windows 发布和 smoke test 均应在本次改动后重新执行。
- [x] 人工验收缺口：发布包中验证左键握手前爪命中、右键特写不下移、零食/狗粮视觉衔接、飞盘近投/远投/过高/不可达、连续取消、访客切换，以及多显示器高 DPI 场景。
- [x] 风险边界：当前 smoke test 会真实启动启用训练犬的发布程序并验证单实例、Named Pipe、显示隐藏、暂停和退出，但不会模拟鼠标输入、命中区域、动画逐帧观感或多显示器布局。