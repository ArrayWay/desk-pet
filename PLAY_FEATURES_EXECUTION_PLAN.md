# 玩法功能执行计划

## 目标

本计划承接现有主宠、单访客槽位、VS Code 双向 IPC 和 M10 养成系统，集中推进一批能增强开发陪伴感、但不会把桌宠变成重型面板或复杂经营游戏的功能。

本轮的共同原则：

- 实用能力优先复用现有命令、事件、状态机和动画；专用素材未就绪时必须有降级体验。
- 主宠、访客和 VS Code 的反馈都保持低打扰，不抢焦点，不阻塞用户输入。
- 所有成长变化继续通过 `GrowthService.ApplyMain/ApplyVisitor` 结算，禁止 UI 或动画逻辑直接修改数值。
- 访客继续使用 `VisitorProfile` 能力门控和 `VisitorSettings.Stats` 分桶；不在 `MainWindow` 中堆叠访客 ID 分支。
- 当前仍保持单访客槽位；“换班”是切换叙事，不是双访客同屏。
- 养成系统关闭时，新增功能不得写入成长数值；已有数据继续封存，重新开启后恢复使用。

## 已确认范围

### 一、VS Code 联动

#### 实用功能

1. **专注双端闭环**
   - 统一 `focus-start` 载荷，明确专注分钟和休息分钟，接通已有 `FocusMinutes`、`BreakMinutes` 设置。
   - 托盘启动、扩展命令启动和自定义专注使用同一套参数校验与计时语义。
   - 桌面端结束专注或进入休息时回传 `focus-state`，扩展侧不再保留过期的专注状态。
   - 专注完成继续通过 `GrowthService` 结算主宠亲密度，并遵守 `GrowthEnabled`。

2. **任务与测试结果反馈**
   - 保留现有 `waiting`、成功、失败和打开 Problems 的动作映射。
   - 任务/测试事件增加任务名称、失败摘要和退出结果的结构化字段。
   - 失败时提供低频气泡或状态提示，并保留一键打开 Problems 的路径；长摘要不得直接塞入桌宠窗口。
   - 事件异常或扩展断线时降级为本地动画，不影响桌宠主循环。

3. **晨间来访语义与 VS Code 首次启动对齐**
   - 扩展激活后发送一次按日期去重的 `morning-check`，表示用户当天第一次进入 VS Code 工作流。
   - 桌面端继续负责实际访客窗口、日期戳和动画生命周期，常驻桌宠进程不再单独抢先消费“今日首次见面”。
   - 桌面端未连接扩展时保留当前启动降级路径，恢复连接后不得重复触发当天事件。

#### 有趣功能

4. **提交仪式**
   - 监听 staged/commit 成功等明确的 Git 成功信号，避免仅因打开 SCM 就触发。
   - 主宠播放现有挥手或庆祝动作，访客在能力允许时播放庆祝反馈。
   - 事件低频去重；亲密度奖励为小额行为结算，养成关闭时只保留动画反馈。

5. **长会话陪伴**
   - 连续编码达到阈值且期间无失败事件时，低概率调度访客主动靠近、半坐或嗅闻。
   - 复用现有空闲调度、冷却和最近行为去重，不增加常驻计时窗口。
   - 专注、拖拽、飞盘、握手等高优先级行为期间不得插入主动陪伴。

### 二、桌宠互动（主宠本体）

#### 实用功能

1. **状态条信息密度**
   - 在现有主宠浅橙色状态条上增加软门槛生效标记、今日专注次数/分钟等可读信息。
   - 信息增加必须保持稳定尺寸、可截断和不遮挡角色；访客蓝色状态条保持独立主题。
   - 状态条只读展示数据，关闭养成系统时整体隐藏并停止刷新。

2. **与精力联动的低打扰休息提醒**
   - 将久坐、喝水、护眼提醒与主宠精力状态组合为分层文案和动作偏好。
   - 低精力时降低提醒侵入性，优先使用坐下、伸懒腰或睡觉等已有动作，不创建硬锁。
   - 提醒仍遵守独立开关、冷却和不抢焦点规则。

3. **回到当前工作区屏幕**
   - 增加“回到工作区”定位动作：优先使用扩展提供的 VS Code 窗口屏幕信息，缺失时使用前台窗口或鼠标所在屏幕的启发式降级。
   - 定位后使用现有边缘吸附与多显示器边界修正，显示器断开时回退到可见屏幕。
   - 该动作只改变窗口位置，不修改养成数值或当前动画状态。

#### 有趣功能

4. **点击连招状态机**
   - 在现有单击轮换和双击跳跃基础上，增加短时间窗口内的 `wave -> jump -> review` 连招。
   - 连招必须有超时、重复点击、拖拽、高优先级事件和窗口失焦清理路径。
   - 互动收益沿用 `GrowthService.ApplyMain` 与软门槛规则，不新增独立奖励模型。

5. **短距离拖拽安抚**
   - 区分短距离轻拖与长距离搬运：轻拖松手后触发亲昵 idle/安抚反馈，长距离仍走现有惯性和边缘吸附。
   - 使用拖拽距离、持续时间和冷却过滤误触；不影响透明 Alpha 命中测试。
   - 养成关闭时保留动作反馈，但不写入亲密度。

6. **备注驱动的空闲气泡**
   - 主宠 `Note` 有值时，空闲阶段以极低概率生成与备注相关的短句气泡。
   - 文本必须经过长度限制和显示名/备注清洗，不将原始长文本直接注入 UI。
   - 遵守气泡开关、冷却和最近内容去重；没有备注时完全不触发。

### 三、访客系统

#### 实用功能

1. **能力互补**
   - 优先为现有 `dog` 增加 `Feeding`，或为 `training-dog` 补齐 `Petting`，以 `VisitorCapabilities` 控制菜单和动作。
   - 接入必须复用通用访客生命周期、资源契约和取消清理，不在主窗口加入按 ID 的专用流程。
   - 新能力没有完整资源时，菜单不显示或回退到已验证的通用反馈。

2. **喂食与软门槛护理循环**
   - 低饱食或低精力时，用低频气泡提示零食/狗粮；不把提示变成强制任务。
   - 喂食后同步访客状态条、成长称号和冷却剩余；具体奖励继续由 `GrowthService.ApplyVisitor` 统一结算。
   - 零食、狗粮、忙碌、中断、冷却和养成关闭均有明确状态，不能遗留食盆、计时器或旧动画。

3. **“找 Bug”产品化**
   - 保留现有嗅闻到 Problems 的主流程，补充今日找 Bug 次数、诊断数量变化和成功/失败反馈。
   - 诊断清零或明显减少时播放轻量成功反馈；问题增加或打开失败时使用安慰反馈，不伪造成功。
   - 统计按访客 Profile 分桶，访客切换后不串数据；养成关闭时可保留行为记录但不结算成长。

#### 有趣功能

4. **握手成功后低概率转圈**
   - 握手成功状态稳定结束后，以低概率接入短转圈庆祝；失败、取消、切换和退出必须跳过并清理。
   - 只在 `Handshake` 能力开启且资源存在时启用，缺少专用帧时保持现有成功动作。

5. **飞盘成功后的叼盘炫耀**
   - 截飞盘成功后，在交还前低概率播放短距离炫耀或半圈动作。
   - 复用现有飞盘状态机和道具窗口，任何高优先级事件都能取消并关闭飞盘窗口。
   - 失败、不可达、过高、连续取消和切换访客不触发炫耀。

6. **半坐招呼主动调度**
   - 训练犬在晨间来访、长会话或切换入场等合适节点低频主动播放 `half-sit-panting`。
   - 使用现有空闲权重、冷却和最近行为去重，不让主动招呼覆盖任务失败、握手或飞盘等高优先级反馈。

7. **单槽位“换班”剧情**
   - 切换访客时用短气泡和入场/离场动作表达旧访客封存、新访客接班。
   - 旧访客统计继续保存在 `Visitor.Stats[visitorId]`，新访客读取自己的 Profile 分桶。
   - 生命周期顺序必须是停止旧窗口与道具、保存状态、切换 Profile、创建新窗口、再触发入场反馈。

8. **新访客物种**
   - 仅在拥有完整身份基准图、图集、能力契约、降级资源和人工验收记录后注册新物种。
   - 优先沿用 `VisitorProfile` 注册表，不预生成或预注册缺少素材的猫、鸟等访客。
   - 新物种仍受单访客槽位、统一切换生命周期和成长分桶约束。

## 分期实施

### P0：现有玩法与养成验收收口

- 完成 M9 飞盘近投、远投、过高、不可达、连续取消、切换清理和高 DPI 多显示器人工验收。
- 完成 M10 长时间运行、状态条主题、养成开关封存/恢复和发布包人工验收。
- 将已发现的异常补入 `tmp` 验证记录或相应 smoke test，先保证现有行为稳定。

### P1：VS Code 专注和事件闭环

- 完成 `focus-start` 参数统一、`focus-state` 回传和 `BreakMinutes` 接线。
- 完成任务/测试结构化结果、失败摘要和 Problems 路径。
- 完成扩展激活后的 `morning-check` 日戳去重。

### P2：主宠可感知互动与训练犬彩蛋

- 完成状态条信息密度、休息提醒分层和“回到工作区”屏幕定位。
- 完成握手转圈、飞盘炫耀、训练犬半坐招呼的能力/资源门控。
- 为高优先级中断、访客切换、养成关闭补齐回归验证。

### P3：玩法联动与轻量叙事

- 完成提交仪式、长会话陪伴、点击连招、短拖安抚和备注气泡。
- 完成访客护理循环、“找 Bug”统计和诊断结果反馈。
- 完成单槽位换班剧情，并验证成长数据不串访客。

### P4：能力补齐与新 Profile 门槛

- 在素材准备后接入 `dog` 喂食或 `training-dog` 摸头，先完成能力互补再评估新物种。
- 只有完整新 Profile 通过资源校验、切换清理、发布打包和多显示器人工验收后，才注册猫、鸟等新访客。

## 主要实现面

| 领域 | 主要文件/符号 | 责任 |
|---|---|---|
| VS Code IPC | `vscode-extension/extension.js`、`MainWindow.xaml.cs`、`PetEventMessage.cs` | 专注、任务/测试、晨间事件与状态回传 |
| 主宠调度 | `MainWindow.xaml.cs`、`GrowthService.cs` | 点击连招、拖拽安抚、休息提醒和成长结算 |
| 状态展示 | `StatusBarWindow.cs`、`PetSettings.cs`、`VisitorSettings.cs` | 主宠/访客状态条、软门槛标记和养成开关 |
| 访客能力 | `VisitorProfile.cs`、`CompanionWindow.cs`、`VisitorSettings.cs` | 能力门控、护理循环、握手/飞盘/找 Bug 行为 |
| 生命周期 | `MainWindow.xaml.cs`、访客切换与道具窗口相关类 | 切换、取消、中断和资源清理 |
| 资源契约 | `shared/companions/`、Profile 资源校验与 smoke test | 素材存在性、降级和发布验证 |

## 验收标准

- 所有新增功能在养成关闭时不改变 `Affection`、`Mood`、`Stamina`、`Satiety` 或访客成长分桶。
- VS Code 断开、桌面端重启、访客切换、窗口隐藏和退出不会造成未关闭的计时器、道具窗口或重复事件。
- 高 DPI、多显示器、显示器断开和发布目录启动路径均通过验证；桌宠不会被状态条或气泡永久遮挡。
- 任务/测试失败不会伪造成功反馈；Problems 打开失败时仍能完成本地降级动画。
- 低概率彩蛋可被冷却、取消和高优先级事件打断，连续运行不会刷屏或锁死状态机。
- 新 Profile 必须通过资源校验、单访客切换、退出清理、发布打包和人工验收后才算完成。

建议验证命令：

```powershell
$env:DOTNET_CLI_HOME = "$PWD\tmp\dotnet-home"
$env:NUGET_PACKAGES = "$PWD\tmp\nuget-packages"
dotnet build .\desktop-wpf\Fuguang.DesktopPet\Fuguang.DesktopPet.csproj -c Release
npm run check --prefix .\vscode-extension
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
.\tools\smoke-test.ps1 -PublishDirectory .\publish\windows\release
```

日常人工验收入口仍为 `publish/windows/release/Fuguang.DesktopPet.exe`。Release 构建完成后必须使用 csproj 的 `SyncLaunchDirectories` 同步发布目录和 `vscode-extension/desktop`，不得用过期的 `bin` 程序判断结果。

## 明确不纳入本轮

- 双访客同屏和多访客并行窗口。
- 养成货币、商城、签到刷数和复杂经济系统。
- 主宠喂食；当前养成范围继续保持“访客喂食 + 主宠互动/专注”。
- 复杂 AI 对话和重做 VS Code Webview 的整套访客动画。
- 没有完整图集和能力契约时预生成或注册新物种。
