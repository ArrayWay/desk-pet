# 养成系统执行计划

## 目标

本计划定义 desk-pet 的第一版养成系统落地方式。目标不是引入复杂的数值经营，而是把现有的互动、喂食、专注和称号系统整理成一套可持续扩展、可感知、低打扰的成长机制。

本轮只实现用户已确认的范围：

- 文案采用“饱食”，代码字段建议使用 `Satiety`。
- 旧访客进度按 50/50 分给 `dog` 与 `training-dog`。
- v1 只做访客喂食 + 主宠互动/专注，不做主宠喂食。
- 低精力/低饱食采用软门槛，允许继续玩，但给出提示和收益惩罚。
- 桌面数字状态条必做，托盘和气泡只作为补充。

## 现状判断

现有代码里已经有亲密度、心情、精力和部分喂食/互动逻辑，但语义混杂：

- 主宠的 `Energy` 同时承担精力和状态恢复语义。
- 访客的 `Energy` 既表示游玩体力，也承担喂食后的恢复。
- 访客的 `Affection`、`Interactions` 和 `Title` 还是全局共享，未按访客分桶。
- 已有的互动动作散落在窗口逻辑里，后续很难统一调参。

结论是：第一版不应继续叠加字段，而应先做一次清晰拆分和统一结算入口。

## 设计原则

- 软门槛优先，不硬锁死桌宠。
- 行为驱动数值，不让 UI 直接改数值。
- 主宠和每只访客都使用独立成长状态。
- 迁移一次完成，旧存档不丢进度。
- 主界面只展示必要信息，避免做成重型面板。

## 数值模型

每个实体维护四类核心状态：

| 字段 | 含义 | 范围 | 说明 |
|---|---|---:|---|
| `Affection` | 亲密度 | 0–10000 | 长期成长，决定称号和部分文案 |
| `Mood` | 心情 | 0–100 | 短期状态，受互动和日常影响 |
| `Stamina` | 精力 | 0–100 | 互动、游玩、追逐的消耗资源 |
| `Satiety` | 饱食 | 0–100 | 喂食恢复、时间缓慢衰减 |

约定如下：

- UI 文案显示“精力”和“饱食”。
- 代码字段使用 `Stamina` 和 `Satiety`。
- 旧字段 `Energy` 在迁移阶段映射为 `Stamina`。
- `Mood` 保留独立语义，不并入饱食。

## 迁移规则

### 主宠迁移

- 旧 `PetSettings.Energy` 迁移到 `Stamina`。
- `Satiety` 缺省初始化为 75，或按后续调参略作保底。
- 迁移后保存新结构，但可保留一个过渡版本的兼容读路径。

### 访客迁移

- 旧的访客扁平字段按 50/50 分给 `dog` 与 `training-dog`。
- 分配项建议为：`Affection`、`Energy`、`Interactions`、`Title`。
- 迁移时按取整规则向下截断，剩余差值由 `dog` 承担。
- 新访客以后全部写入按 `VisitorProfile.Id` 分桶的统计结构。

### 兼容策略

- 读旧档时自动迁移，保存时写入新结构。
- 一轮过渡期内，代码仍可兼容旧字段读取，但不再继续往旧字段堆新逻辑。
- 迁移失败时回退默认值，不阻塞主宠启动。

## 实现分期

### P0 数据拆分与存档迁移

目标是先把数据结构理顺，再开始改玩法。

- 主宠拆出 `Stamina` 和 `Satiety`。
- 访客统计改为按访客 ID 分桶。
- 完成旧字段到新字段的一次性迁移。
- 补齐 Normalize / Load / Save 兼容路径。

验收标准：旧存档可以正常加载并写回新结构；主宠和不同访客的成长数据不再互相串用。

### P1 行为统一结算

目标是让互动、喂食和专注都走统一数值入口。

- 访客喂食拆分为零食和狗粮两类行为。
- 主宠互动、专注完成、访客互动、握手、取球、飞盘等动作统一结算亲密度、精力、饱食和心情。
- 窗口层只发事件，不直接写散落的 `+=` 或 `-=` 逻辑。

验收标准：同一行为无论从托盘、气泡还是窗口触发，数值结果一致。

### P2 时间衰减、恢复和软门槛

目标是让养成有节奏，但不造成强制中断。

- 精力按低频恢复，游玩时消耗。
- 饱食按低频衰减，喂食回升。
- 低精力时提示休息，但仍允许继续玩，收益降低。
- 低饱食时降低亲密度收益，并提示“有点饿了”。
- 双低时优先推荐休息动画。

验收标准：低状态下仍可继续交互，但会明显感知到收益变化与提示文案。

### P3 桌面状态条与提示

目标是把养成状态做成桌面可见，但不占太多屏幕。

- 增加主宠与当前访客的桌面数字状态条。
- 状态条优先展示：亲密度、精力、饱食、称号或简短状态。
- 托盘菜单和气泡文案只作为补充，不承担主展示职责。
- 若后续要做开关，状态条默认应可关闭。

验收标准：用户不打开托盘也能快速看懂当前养成状态。

### P4 调参与发布前验收

目标是把首版数值调到可长期使用的区间。

- 调整行为增减值、恢复间隔和软门槛阈值。
- 复核日上限，避免刷数值。
- 验证切换访客、隐藏、退出和高优先级事件能完整打断养成动作。
- 验证多显示器、高 DPI 和发布包中的行为一致性。

验收标准：长时间运行不串状态、不残留计时器、不因低状态把桌宠卡死。

## 建议数值表

以下是第一版建议值，后续可按体验再调：

| 行为 | 亲密度 | 精力 | 饱食 | 备注 |
|---|---:|---:|---:|---|
| 主宠单击互动 | +1 | 0 | -1 | 轻反馈 |
| 主宠双击 | +1 | -2 | -1 | 保留双击节奏 |
| 主宠专注完成 | +2 | -5 | -3 | 与专注联动 |
| 访客轻互动 / 抚摸 | +1~2 | -2 | -1 | 轻量正反馈 |
| 访客取球 | +3 | -12 | -4 | 保持现有消耗基准 |
| 访客飞盘成功 | +4 | -20 | -6 | 维持高消耗高收益 |
| 访客飞盘失败 | +1 | -20 | -6 | 失败也有小反馈 |
| 喂零食 | +1 | +2 | +10 | 无冷却或短冷却 |
| 喂狗粮 | +2 | +4 | +18 | 保留冷却 |

## 称号与阶段

第一版保留现有访客称号体系，并按访客自身统计独立计算：

- 新朋友
- 好伙伴
- 最佳搭档

如果后续需要，再为主宠增加轻量阶段文案，但第一版不强制新增门槛型系统。

## 风险点

- 旧 `Energy` 的双义语义如果拆得不彻底，后续调参会继续混乱。
- 访客按 ID 分桶后，迁移取整规则需要明确，否则用户会感觉进度丢失。
- 桌面状态条如果信息过多，会反过来破坏低打扰目标。
- 行为结算不集中，后续新增动作会重新长出散落逻辑。

## 当前决定

已锁定的实现边界如下：

- 文案使用“饱食”。
- 代码字段使用 `Satiety`。
- 旧访客进度按 50/50 分配给 `dog` 与 `training-dog`。
- v1 只做访客喂食 + 主宠互动/专注。
- 低精力 / 低饱食是软门槛。
- 桌面数字状态条必须实现。

## 实现状态（代码）

| 分期 | 状态 | 说明 |
|---|---|---|
| P0 数据拆分与迁移 | 已完成 | `PetSettings.Stamina/Satiety`；`VisitorSettings.Stats`；旧 `Energy` 读迁移；访客 50/50（余数给 dog） |
| P1 统一结算 | 已完成 | `GrowthService` + `GrowthAction`；`MainWindow` 主宠点击/双击/专注与访客喂食/取球/飞盘等走统一入口 |
| P2 衰减/软门槛 | 已完成 | 8 分钟饱食衰减；低精力/低饱食软惩罚与气泡提示；取球/飞盘不再硬拦截 |
| P3 桌面状态条 | 已完成 | `StatusBarWindow`；主宠/访客跟随；托盘「显示状态条」；默认开启 |
| P4 调参与验收 | 部分完成 | 自动回归 tmp/growth_p4_smoke.py；软门槛对 +1 亲密度可降为 0；RegisterActivity/MoodTimer 收口到 GrowthService；状态条上方布局与重叠分离。仍待：长时间运行、多显示器/高 DPI、发布包人工验收 |


补充（状态条主题与养成开关）：

- 主宠状态条浅橙质感、访客状态条蓝色质感。
- PetSettings.GrowthEnabled：关闭后 GrowthService 不再结算，数值封存；托盘「养成系统」可开关；开启后可再选显示/隐藏状态条。

主要新增/改动文件：

- `desktop-wpf/Fuguang.DesktopPet/GrowthService.cs`
- `desktop-wpf/Fuguang.DesktopPet/VisitorGrowthStats.cs`
- `desktop-wpf/Fuguang.DesktopPet/StatusBarWindow.cs`
- `desktop-wpf/Fuguang.DesktopPet/PetSettings.cs`
- `desktop-wpf/Fuguang.DesktopPet/VisitorSettings.cs`
- `desktop-wpf/Fuguang.DesktopPet/MainWindow.xaml.cs`

验证：

```powershell
$env:DOTNET_CLI_HOME = "$PWD\tmp\dotnet-home"
$env:NUGET_PACKAGES = "$PWD\tmp\nuget-packages"
dotnet build .\desktop-wpf\Fuguang.DesktopPet\Fuguang.DesktopPet.csproj -c Release
```

## 后续建议

P4 优先：按建议数值表微调、跑旧档迁移人工确认、检查状态条在隐藏/退出/切换访客时无残留窗口。