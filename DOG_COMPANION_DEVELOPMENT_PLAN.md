# 真实宠物狗玩伴开发计划

## 产品定位

为浮光橙仔增加一只写实宠物狗玩伴。狗狗使用真实宠物摄影风格和透明背景，不采用 Q 版、卡通化或拟人化造型。玩伴是独立窗口和独立状态机，可以召唤、送回、跟随、休息和响应开发事件，不与主宠图像合并。

狗狗定位为偶尔出现、会回应开发事件、能和主宠建立关系的玩伴，而不是持续跟在旁边的装饰。系统利用现有 15 个主宠动画状态、亲密度、心情、精力、专注计时和 VS Code 事件联动，形成轻量双宠互动。用户可从托盘主动召唤，也可开启自动来访；默认不持续占用屏幕空间。

## 设计约束

- 保持真实犬只的外貌、毛色、体型和项圈一致，不使用卡通比例。
- 同一时间最多显示一个玩伴，避免遮挡编辑器和桌面。
- 默认低打扰；专注期间不追逐、不频繁触发动作。
- 玩伴窗口独立于主宠窗口，后续可替换为其他犬只或其他物种。
- 默认自动来访关闭；来访有频率上限和离开条件，用户主动召唤除外。
- 同时最多显示一个访客玩伴；新增猫、鸟等玩伴后沿用统一访客槽位。
- 优先复用现有开发事件、专注、心情、精力和亲密度系统。
- 素材按阶段补充，只生成当前里程碑必需的图片。
- 图片和构建产物保存在项目目录，新增工具与依赖遵守 D 盘存储约束。

## 素材规范

- 第一阶段只需要一张狗狗身份基准图：完整全身、侧前方视角、自然站立、透明背景。
- 后续动作必须以身份基准图作为参考，锁定品种、脸型、毛色花纹、耳朵、尾巴、体型和项圈。
- 每个动画单元最终适配 `192x208`，主体四周保留安全边距，不要裁掉耳朵、尾巴或脚掌。
- 禁止文字、场景、地面、投影、光晕、玩具和漂浮特效，除非具体玩法明确需要道具。
- 图像使用自然柔和的棚拍光线，毛发边缘清晰，便于透明化处理。

## 里程碑

### C1 玩伴基础框架

- [x] 制定真实宠物狗素材规范和分阶段生成方案。
- [x] 验收真实宠物狗身份基准图并接入 `shared/companions/dog.png`。
- [x] 增加独立透明 `CompanionWindow`。
- [x] 增加“召唤狗狗 / 送狗狗回家”托盘菜单。
- [x] 保存玩伴启用状态、自动来访、亲密度和精力。
- [x] 缺少素材时给出明确提示，不影响主宠启动。
- [x] 狗狗跟随主宠，停止后保持在主宠侧后方。
- [x] 主宠隐藏或退出时同步隐藏或退出狗狗。

验收标准：放入一张符合规范的狗狗 PNG 后，无需改代码即可召唤；狗狗平滑跟随主宠且不会移出当前工作区。

### C2 基础互动

- [x] 首只狗左键单击触发基础互动；训练犬左键单击改为限时握手邀请，避免与摸头入口冲突。
- [x] 双击狗狗触发跳跃、靠近主宠或绕主宠跑一圈，并增加亲密度。
- [x] 首只狗保留长按摸头；训练犬改由右键触发摸头回应并增加少量亲密度，右键特写只轻微放大脸部且不改变窗口垂直位置。
- [x] 短时间反复点击时进入躲闪或困惑状态，不无限重复同一动作。
- [x] 增加狗狗坐下、趴下和睡觉的空闲调度。
- [x] 使用当前帧 Alpha 命中测试，透明区域不响应摸头和点击。

验收标准：互动有冷却时间，不会因连续点击卡住；透明区域不误触。

### C3 开发事件联动

- [x] 运行构建或测试时，狗狗进入等待姿态。
- [x] 构建成功时狗狗播放庆祝动作并恢复陪伴状态。
- [x] 构建失败时狗狗垂耳靠近主宠，点击任意一只都可打开 Problems。
- [x] 测试全部通过时触发一次短暂追逐或碰爪庆祝。
- [x] 专注开始后狗狗坐在主宠旁边守候，降低移动和主动互动频率。
- [x] 专注期间检测高频窗口切换或中断，狗狗抬头观察，但不弹出阻塞提醒。
- [x] 专注完成后两只宠物一起伸懒腰或庆祝。
- [x] 长时间无操作时狗狗靠近主宠；主宠坐下时狗狗趴在旁边。
- [x] 主宠低精力自动睡觉时狗狗逐渐入睡，鼠标靠近狗狗时先醒来再唤醒主宠。

验收标准：玩伴只响应当前最高优先级事件，动作结束后恢复跟随或休息状态。

### C4 玩耍系统

- [x] 托盘增加“和狗狗玩球”，进入一次性投球模式。
- [x] 用户点击桌面可用位置确定球的落点。
- [x] 狗狗跑向球、拾取并返回主宠附近。
- [x] 成功取回增加主宠与狗狗的亲密度。
- [x] 连续玩耍消耗狗狗精力，低精力时拒绝继续并趴下休息。
- [x] 道具球使用独立轻量窗口，跟随状态结束时自动清理。

验收标准：道具不会离开可用工作区；玩耍期间仍可中断并响应退出、隐藏和高优先级提醒。

### C5 跟随、追逐与关系反馈

- [x] 狗狗在主宠附近自主活动，保持最小和最大跟随距离。
- [x] 主宠快速移动时狗狗延迟追上，不与主宠窗口完全重叠。
- [x] 主宠停止后狗狗在侧后方坐下或趴下。
- [x] 主宠心情低时狗狗主动靠近安慰。
- [x] 主宠心情高且狗狗精力充足时，两只宠物低频追逐。
- [x] 狗狗行为反向影响主宠心情，但设置每日上限，避免刷数值。

验收标准：跟随过程平滑，不抖动、不越过工作区；关系反馈有冷却且不会覆盖开发失败、专注完成等高优先级事件。

### C6 彩蛋与长期玩法

- [x] “找 Bug”：可让狗狗进入嗅闻状态，点击狗狗打开 Problems。
- [x] “找 Bug”素材回归：修复原 2x3 参考图按等宽槽提取时切断横向主体的问题；改为按整行连通主体排序提取，6 帧完整性与透明安全边缘验证通过。
- [x] “藏起来”：狗狗按小时级冷却低频播放藏边探头动作。
- [x] “晨间来访”：每天第一次启动 VS Code 时，狗狗跑来迎接主宠，每天最多一次。
- [x] “下班提醒”：连续工作过久后，狗狗叼来玩具或牵引绳并触发休息提醒。
- [x] “心情感染”：主宠心情低时狗狗低频靠近安慰。
- [x] “小冲突”：狗狗低频抢走主宠小道具，主宠追赶；可被高优先级事件立即中断。
- [x] “成长与称号”：按有效互动次数解锁“新朋友”“好伙伴”“最佳搭档”，不增加复杂养成货币。
- [x] “访客系统”：已建立 `VisitorProfile`、能力声明和 `VisitorSettings`，完成通用设置替换；`CompanionWindow` 通过 Profile 加载资源、解析逻辑动作和门控可选玩法；主窗口通过 `ActiveVisitorId` 持有一个活动访客槽位，并由当前 Profile 生成菜单、事件动作和反馈文案。狗狗作为首个 Profile；后续猫、鸟只需注册 Profile 与资源，不在本轮新增素材或选择界面。

验收标准：彩蛋均可关闭并具有日级或小时级冷却；不会因未连接 VS Code、缺少道具素材或中途隐藏而残留窗口。

### C7 发布与质量保证

- [x] 桌面端与 VS Code 扩展同步版本（`1.2.1`）。
- [x] v1.2.1 同步记录主宠自动运行开关、主宠人物2换装和换装后互动身份保持修复。
- [x] 覆盖单实例、显示隐藏和 IPC smoke test。
- [x] smoke test 验证发布包包含狗狗 Profile 的基础图、球和全部动作目录，并验证新设置只持久化通用 `Visitor` 结构。
- [x] 在 100% 和高 DPI 缩放下完成访客初始位置、跟随、跨屏、显示器断开与球落点的人工验收。
- [x] 对全部狗狗动作执行透明度、边界和逐帧检查。
- [x] 发布包包含完整狗狗动作资源；缺少可选玩伴素材时主宠仍可启动。
- [x] Release Windows 包与 `fuguang-orange-pet.vsix` 已重新生成并通过自动化验证。
- [x] M8 接入阶段已增加 Profile 注册表和启动前资源校验；当前狗狗资源校验通过，后续访客不满足资源契约时会在启动入口给出明确诊断。
- [x] Release smoke test 已增加逐状态 PNG 帧检查，并在重新发布的 Windows Release 上通过；VSIX 同步包含当前访客资源。
- [x] 主窗口已记录活动 Profile ID；未来切换访客时会先停止旧窗口、互动计时和球窗口，再创建新访客。当前单狗 Release smoke test 通过。
- [x] 注册数大于 1 时才显示访客选择菜单；当前狗狗仍是唯一 Profile，因此现有单狗菜单不增加选择项。第二个访客接入后需补充切换、能力菜单和资源降级人工验收。
- [x] 后续访客统一按 [VISITOR_PROFILE_GUIDE.md](VISITOR_PROFILE_GUIDE.md) 接入，不在身份素材确认前生成或注册猫、鸟占位资源。

### C8 第二只狗的差异化互动

第二只狗定位为精力更旺盛、会主动邀请用户训练的伙伴。核心记忆点不是复刻首只狗的摸头、找 Bug 和普通取球，而是通过身体姿态和道具形成短而完整的互动回合。

#### 第一阶段：近距离训练

- [x] “半坐伸舌”：已作为第二只狗的招牌待机状态接入 `training-dog` Profile；后续补充低频主动招呼调度。
- [x] “握手邀请”：已接入托盘入口和 2 秒邀请状态；训练犬左键单击启动邀请，邀请期间点击身体播放疑惑反馈，点中前爪进入成功状态。
- [x] “握手成功”：已接入成功动作、8 秒冷却、少量亲密度和有效互动回调；待发布包人工确认前爪命中区。
- [x] “喂食”菜单拆分为“喂零食（无冷却）”和“喂狗粮”；零食只增加少量亲密度和心情，不恢复精力，狗粮继续恢复少量精力。
- [x] “喂狗粮”：托盘单击后直接开始；狗狗先嗅闻，再低头进食、舔嘴感谢，不再要求二次点击放置食盆。
- [x] `food-sniff` 与 `eating` 动作已将狗和食盆合成在同一透明帧中，不再创建独立顶层食盆窗口；切换访客、隐藏、退出或高优先级事件发生时立即停止计时器并恢复状态。
- [x] 只有狗粮设 30 秒冷却且冷却失败显示剩余秒数；零食没有冷却，但播放动作期间仍拒绝重入，避免状态互相覆盖。不引入狗粮库存、货币、每日签到或连续投喂刷数值。
- [x] 专用零食使用 `second-dog-treat-eating-reference.png` 提取的 `treat-eating` 六帧，表现接取、咀嚼与舔鼻动作；零食不进入狗粮冷却。

第一阶段验收标准：半坐伸舌可以自然循环；左键握手必须由用户点中抬起的前爪完成，右键摸头使用 `petting-response` 并只做轻微脸部特写；零食没有冷却且与狗粮在菜单、反馈和数值上明确区分；喂狗粮帧中的食盆与狗自然对齐且不出现重复图层；所有互动都能被构建失败、专注完成、隐藏和退出中断。代码、资源和自动化验证已完成；前爪命中手感、右键特写观感、喂食合成观感及高 DPI 行为仍需发布包人工验收。

#### 第二阶段：截飞盘

- [x] 用户进入飞盘模式后，通过按下与拖动确定方向和力度，释放后飞盘沿受限抛物线飞行。
- [x] 狗狗先观察飞盘方向，再跑向预测交点，而不是追逐飞盘当前坐标。
- [x] 接近交点时播放腾空截飞盘；成功后衔接落地、叼盘返回和放下飞盘，失败时播放扑空回望。
- [x] 飞盘轨迹和狗狗活动范围始终限制在目标显示器可用工作区；跨屏抛掷按落点所在屏幕重新约束。
- [x] 截飞盘消耗 20 点精力，高于普通取球的 12 点；低精力时狗狗趴下拒绝继续。
- [x] 飞盘使用独立道具图和窗口，不复用球图；流程可被更高优先级事件中断，并完整释放轨迹计时器与道具窗口。

第二阶段验收标准：飞盘成功与失败都能稳定结束并恢复跟随；不同方向和力度下至少覆盖近投、远投、过高和落点不可达四类结果；连续开始、取消或切换访客不残留飞盘。

#### 推荐扩展动作

- “碰鼻”：单击鼻尖触发短促后仰、重新靠近和眨眼，适合作为无道具彩蛋。
- “转圈听令”：握手成功后低概率主动转一圈，作为训练连招，不单独增加菜单入口。
- “顶狗粮忍耐”：狗粮短暂放在鼻梁前保持，用户再次点击才允许吃；作为高亲密度低频动作，不进入第一阶段。
- “叼飞盘炫耀”：截盘成功后不立即归还，绕主宠半圈再放下；设置低概率且可跳过，避免拖长每次回合。

#### 新素材清单

第一阶段至少需要 `half-sit-panting`、`handshake-offer`、`handshake-success`、`food-sniff`、`eating` 和 `licking-thanks` 六套动作，以及独立 `food-bowl.png`。第二阶段需要 `frisbee-watch`、左右助跑、左右腾空截盘、`frisbee-landing`、左右叼盘返回和 `frisbee-miss`，以及独立 `frisbee.png`。仅当狗狗花纹、项圈和光照镜像后仍正确时，才由右向动作生成左向动作。

#### 工程边界

- 新增通用能力建议拆分为 `Feeding`、`Handshake` 和 `FrisbeeCatch`，由 Profile 声明真实具备的玩法。
- 新动作加入通用 `VisitorState` 和 Profile 状态映射；第二只狗可以省略首只狗的找 Bug、藏边探头等非必要状态。
- 道具资源契约从仅有球扩展为按能力校验狗粮和飞盘；缺少声明能力所需素材时拒绝激活该 Profile。
- 首只狗行为与资源保持不变；不得通过访客 ID 条件判断改变共享生命周期。

推进顺序：第二只狗身份素材和两阶段动作已完成提取、QA 和正式部署；第二阶段已接入 Profile 资源契约、飞盘投掷层、受限轨迹、预测截点及成功/失败恢复，当前进入发布包人工交互验收。

第一阶段自动化记录：Release 构建、扩展检查、9 组正式帧 QA、Windows/VSIX 发布和增强 smoke test 均已通过。训练犬基础图的 Profile 相对路径已修正为 `training-dog\training-dog.png`；被裁断的食盆已重建为完整透明资源，并由确定性脚本合成进 `food-sniff`、`eating` 正式帧，与狗保持同一位图层级。喂食现由托盘单击直接触发，30 秒冷却会显示剩余时间，不再创建独立食盆窗口；专用 `treat-eating` 帧已接入零食流程。首狗 `sniffing-right` 也已改用整行连通主体模式重建，解决“找 Bug”横向姿态被等宽分槽切断的问题。增强 smoke test 除验证基础图、`food-bowl.png` 和 9 个动作目录外，还会以启用的 `training-dog` 设置启动发布程序，覆盖实际 Profile 资源校验和窗口创建。前爪命中手感、喂食合成观感和高 DPI 行为保留为人工验收项。

第二阶段自动化记录：9 组飞盘动作共 54 帧和独立 `frisbee.png` 已通过透明帧 QA；Release 构建、扩展检查、Windows/VSIX 发布和增强 smoke test 均已通过。增强 smoke test 会验证发布包中的飞盘道具及每组 6 帧动作，并真实启动启用 `training-dog` 的发布程序。近投、远投、过高、不可达、连续开始取消、访客切换及多显示器高 DPI 手感保留为人工验收项。

## 当前素材批次

身份基准图和全部动作参考表已提供，并已通过项目脚本提取为运行时帧。原始素材保存在 `source-picture/dog-companion/`，处理后的素材保存在 `shared/companions/dog/`；提示词和文件对应关系见 [DOG_COMPANION_IMAGE_PROMPTS.md](DOG_COMPANION_IMAGE_PROMPTS.md)。

运行时动作包括 `idle`、`running-right`、`running-left`、`sitting`、`lying-down`、`sleeping`、`waking-stretch`、`happy-celebration`、`sad`、`petting-response`、`confused-dodge`、`waiting`、`guarding`、`comforting`、`sniffing-right`、`peeking`、`carrying-ball-right`、`carrying-ball-left` 和独立玩具球。`running-left` 与 `carrying-ball-left` 由对应右向动作镜像生成；成长称号、访客系统和数值反馈不需要额外图片。访客系统实施时，现有 `DogCompanion*` 设置字段将替换为通用访客状态字段；旧设置文件中的狗狗亲密度、精力、称号和冷却不会迁移，升级后按默认值初始化。

## 行为优先级

从高到低统一为：退出或隐藏、构建失败、专注完成与提醒、构建和测试进行中、用户直接互动、抛球、关系反馈、空闲行为和彩蛋。高优先级事件可以中断低优先级玩法，结束后恢复跟随、坐下或离开状态。

## 双宠数值规则

- 狗狗使用独立的亲密度和精力；主宠继续使用现有亲密度、心情和精力。
- 摸头、取球、找到藏起来的狗狗和完成共同专注可增加狗狗亲密度。
- 追逐、取球和长时间来访消耗狗狗精力；坐下、趴下、睡觉和离开期间恢复。
- 重复点击、重复取球和反复触发同一事件采用冷却与递减收益。
- 关系称号只由有效互动次数和亲密度阶段决定，不引入食物、货币或付费资源。

## 素材交付记录

狗狗身份基准图已经保存在 `source-picture/dog-companion/dog-reference.png`，处理后的基础运行时资源位于 `shared/companions/dog.png`。完整动作帧和球道具位于 `shared/companions/dog/`；原始参考表与处理后的运行时资源均已接入发布流程。
